= @user.email
- unless @user == current_user
  = link_to 'talk with', message_path(@user)

- if current_user == @user
  = form_for @user, url: user_path, method: :put do |f|
    = f.text_field :name
    = f.submit 'Editer'